# --- Web framework ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3

# --- LangGraph agent + LLM ---
langgraph>=0.2.50
langgraph-checkpoint-mongodb>=0.1.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
groq>=0.4.0

# --- Google Sheets ---
gspread>=6.1
google-auth>=2.30

# --- Database (sessions / chat history) ---
pymongo>=4.8

# --- Misc ---
requests>=2.32
python-dotenv>=1.0

# --- Optional audio transcription (only used if TRANSCRIPTION_PROVIDER=openai) ---
openai>=1.40
